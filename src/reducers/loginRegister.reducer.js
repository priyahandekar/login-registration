const initialState = {
    username: "",
    password: "",
    submitting: false
};

const loginRegister = (state=initialState, action) => {
    switch(action.type) {
        default: return state;
    }
}

export default loginRegister;